<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.harness</groupId>
  <artifactId>agentic-harness-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Agentic Harness (JVM)</name>
  <description>
    A build-enforced quality harness for directing coding agents across a multi-module JVM
    codebase. The gates are the product; the sample services exist only to exercise them.
  </description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <junit.version>5.11.4</junit.version>
    <assertj.version>3.26.3</assertj.version>
    <archunit.version>1.3.0</archunit.version>
    <spring-web.version>7.0.9</spring-web.version>
    <jakarta-persistence.version>3.1.0</jakarta-persistence.version>

    <jacoco.version>0.8.12</jacoco.version>
    <checkstyle-plugin.version>3.6.0</checkstyle-plugin.version>
    <checkstyle.version>10.20.1</checkstyle.version>
    <spotbugs.version>4.10.4.1</spotbugs.version>
    <findsecbugs.version>1.13.0</findsecbugs.version>
    <spotless.version>2.43.0</spotless.version>
    <pitest.version>1.17.0</pitest.version>
    <pitest-junit5.version>1.2.1</pitest-junit5.version>
    <surefire.version>3.5.2</surefire.version>
    <dependency-check.version>13.0.0</dependency-check.version>

    <!-- The core lifecycle plugins. Unpinned, these come from whatever Maven the
         runner ships, so the same commit compiles and packages differently on two
         machines and the gates see different bytecode. -->
    <compiler-plugin.version>3.16.0</compiler-plugin.version>
    <resources-plugin.version>3.5.0</resources-plugin.version>
    <jar-plugin.version>3.5.1</jar-plugin.version>
    <install-plugin.version>3.1.4</install-plugin.version>

    <!-- The coverage floor. Ratchet upward, never downward. See README, "The ratchet". -->
    <coverage.line.minimum>1.00</coverage.line.minimum>
    <!-- The mutation floor. Line coverage alone does not prove the tests assert anything. -->
    <mutation.threshold>85</mutation.threshold>
  </properties>

  <modules>
    <module>harness-rules</module>
    <module>sample-order-service</module>
    <module>sample-ledger-service</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit</artifactId>
        <version>${archunit.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
        <version>${spring-web.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context</artifactId>
        <version>${spring-web.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.persistence</groupId>
        <artifactId>jakarta.persistence-api</artifactId>
        <version>${jakarta-persistence.version}</version>
      </dependency>
      <dependency>
        <groupId>io.harness</groupId>
        <artifactId>harness-rules</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${resources-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${jar-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-install-plugin</artifactId>
          <version>${install-plugin.version}</version>
        </plugin>
        <!-- Gate 8 (sensor), invoked from .github/workflows/owasp-weekly.yml rather
             than bound to a phase. Pinned here so the weekly run cannot silently
             pick up a new major that reinterprets failBuildOnCVSS. The workflow
             repeats the version in its GAV; keep the two in step. -->
        <plugin>
          <groupId>org.owasp</groupId>
          <artifactId>dependency-check-maven</artifactId>
          <version>${dependency-check.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Gate 1 (sensor): line coverage, enforced per package, halts the build. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
            <!-- Explicit, so -Djacoco.skip=true cannot turn the gate off. POM
                 configuration wins over a plugin's user property. -->
            <configuration>
              <skip>false</skip>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>jacoco-check</id>
            <phase>test</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <skip>false</skip>
              <rules>
                <rule>
                  <element>PACKAGE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${coverage.line.minimum}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
              <haltOnFailure>true</haltOnFailure>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Gate 2 (guide): formatting is applied, not policed. Drift self-heals. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <googleJavaFormat>
              <version>1.22.0</version>
              <style>GOOGLE</style>
            </googleJavaFormat>
            <removeUnusedImports/>
            <trimTrailingWhitespace/>
            <endWithNewline/>
          </java>
        </configuration>
        <executions>
          <execution>
            <id>spotless-apply</id>
            <phase>validate</phase>
            <goals><goal>apply</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- Gate 3 (sensor): style violations fail the build at validate. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle-plugin.version}</version>
        <dependencies>
          <dependency>
            <groupId>com.puppycrawl.tools</groupId>
            <artifactId>checkstyle</artifactId>
            <version>${checkstyle.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <configLocation>${maven.multiModuleProjectDirectory}/checkstyle.xml</configLocation>
          <consoleOutput>true</consoleOutput>
          <violationSeverity>error</violationSeverity>
          <includeTestSourceDirectory>true</includeTestSourceDirectory>
          <failOnViolation>true</failOnViolation>
          <!-- Both documented off-switches, closed. -Dcheckstyle.skip=true skips
               the check; -Dcheckstyle.skipExec=true leaves it reading a stale
               result file instead of the source. Neither is honoured now. -->
          <skip>false</skip>
          <skipExec>false</skipExec>
        </configuration>
        <executions>
          <execution>
            <id>checkstyle-validate</id>
            <phase>validate</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- Gate 4 (sensor): static analysis including security detectors, at verify. -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.version}</version>
        <configuration>
          <excludeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</excludeFilterFile>
          <effort>Max</effort>
          <!-- Max effort, medium threshold. Tightening to Low was tried and reverted
               on 2026-09-08: the Low band reports MC_OVERRIDABLE_METHOD_CALL_IN_CONSTRUCTOR
               against `this(LedgerService::saleEntries)`, and saleEntries is public
               STATIC, so it cannot be overridden. Going green would need either an
               exclusion (a gate change) or production code contorted around a detector
               bug. See .deep-dive/deferrals.md. -->
          <threshold>Medium</threshold>
          <failOnError>true</failOnError>
          <!-- Explicit, so -Dspotbugs.skip=true cannot turn the gate off. -->
          <skip>false</skip>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>com.h3xstream.findsecbugs</groupId>
            <artifactId>findsecbugs-plugin</artifactId>
            <version>${findsecbugs.version}</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>spotbugs-check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Gate 5 (sensor): mutation testing. Opt-in because it is slow: mvn -Pmutation verify -->
    <profile>
      <id>mutation</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>${pitest.version}</version>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>${pitest-junit5.version}</version>
              </dependency>
            </dependencies>
            <configuration>
              <targetClasses><param>io.harness.*</param></targetClasses>
              <targetTests><param>io.harness.*</param></targetTests>
              <mutationThreshold>${mutation.threshold}</mutationThreshold>
              <!-- PIT's off-switches are -DskipPitest=true and the common
                   -DskipTests. Configured here, neither is honoured. -->
              <skip>false</skip>
              <skipTests>false</skipTests>
              <timestampedReports>false</timestampedReports>
              <outputFormats>
                <outputFormat>HTML</outputFormat>
                <outputFormat>XML</outputFormat>
              </outputFormats>
            </configuration>
            <executions>
              <execution>
                <id>pitest-check</id>
                <phase>verify</phase>
                <goals><goal>mutationCoverage</goal></goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
