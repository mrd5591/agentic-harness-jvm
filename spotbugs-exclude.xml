<?xml version="1.0" encoding="UTF-8"?>
<!--
  Every entry here is a deliberate, justified exemption. Adding to this file is a
  review event: it is how a gate gets weakened, so it should be as visible as a
  production change. Keep the reason inline.

  Currently empty, on purpose. Test sources are not analysed at all (the plugin's
  includeTests defaults to false), so no test-source filter is needed, and no
  production finding has yet earned an exemption.
-->
<FindBugsFilter>
</FindBugsFilter>
